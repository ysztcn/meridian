# Model to use for embeddings (example)
EMBEDDING_MODEL_NAME=intfloat/multilingual-e5-small

# Port for local development (if not using Cloud Run's $PORT)
# PORT=8080

# API Token for auth
# API_TOKEN=hunter2